# Проверка 0.1.7

Дата: 2026-09-09.

- Целевой прогон: 37 тестов управления сеансом и концом записи, все PASS.
- Полный прогон финального кода: 123 unit tests PASS, 0 failures/errors; release lint: 0 ошибок, 12 предупреждений. Сборка завершилась успешно за 31,93 с. Полный прогон для этого кода выполнен один раз.
- Проверены подпись APK v2 и zipalign; сертификат совпадает с предыдущим установленным прототипом.
- Тесты покрывают раннюю паузу у конца записи, смену названия и media ID очереди, ожидание свежего подтверждения паузы, повторный старт очереди во время подтверждения, неизвестную длительность, отсутствие поддержки паузы, сохранение службы в обычном режиме и ожидание нового источника в Live Sync.

APK: 155208 байт, SHA256 `8f77378ab97e3d2e4a4d05602f1386fe14e5d1a1b5419358b9cc01a919284cb7`.

Обновление OnePlus 12 / Android 16 установлено: versionCode 8, versionName 0.1.7; SHA256 установленного APK совпадает с указанным выше. Первая USB-установка зависла при заблокированном экране; отменена только её временная сессия. После разблокировки тот же APK установлен без повторного полного прогона тестов.

Первый контрольный подхват на 0.1.7 при очень слабом сигнале микрофона не распознал песню и завершился через 35,06 с. При повторе с музыкой три позиции согласовались с разбросом 14 мс; разница таймкодов после перемотки составила −2 мс. При принудительном переходе очереди следующая запись действительно встала на паузу на позиции 0, однако приложение ложно сообщило об отсутствии подтверждения: время обновления позиции осталось старым. Исправление этого подтверждения включено в 0.1.8. Проверка таймкодов не доказывает совпадение слышимого звука.

Автоматическая акустическая коррекция и точность до миллисекунды не реализованы и не заявляются. Проверка трёх фрагментов подтверждает согласованность опорной позиции; она не устраняет систематический сдвиг распознавателя или звукового выхода.
